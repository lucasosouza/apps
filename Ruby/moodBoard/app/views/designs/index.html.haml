%p#notice= notice
%h1 Listing Designs
%table
  %thead
    %tr
      %th Title
      %th{:colspan => "3"}
  %tbody
    - @designs.each do |design|
      %tr
        %td= design.title
        %td= image_tag design.photo.url(:thumb)
        %td= link_to 'Show', design
        %td= link_to 'Edit', edit_design_path(design)
        %td= link_to 'Destroy', design, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Design', new_design_path
